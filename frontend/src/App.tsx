import { useEffect, useMemo, useRef, useState } from 'react'
import { db } from './db'
import * as api from './api'
import type { Category, Member, Transaction, TxType } from './types'
import { nowIso, monthRange, today, uid, yuan } from './utils'
import { ResponsiveContainer, PieChart, Pie, Cell, Tooltip } from 'recharts'

const defaultTx = (): Partial<Transaction> => ({ id:uid(), project:'', date:today(), member:'', type:'expense', category1:'', category2:'', amount_cents:0, note:'', created_at:nowIso(), updated_at:nowIso(), device_id:api.deviceId(), is_deleted:false })
const PIE_COLORS=['#111827','#2563eb','#059669','#d97706','#dc2626','#7c3aed','#db2777','#0891b2','#65a30d','#9333ea']
const moneyLabel=(value:number)=>`¥${Math.round(Number(value)).toLocaleString('zh-CN')}`
const sum=(xs:Transaction[])=>xs.reduce((s,t)=>s+t.amount_cents,0)
const periodRange=(kind:'week'|'month'|'year')=>{
  const d=new Date(); const y=d.getFullYear(); const m=d.getMonth(); const day=d.getDate()
  if(kind==='year') return {start:`${y}-01-01`,end:`${y}-12-31`}
  if(kind==='month') { const end=new Date(y,m+1,0); return {start:`${y}-${String(m+1).padStart(2,'0')}-01`,end:`${y}-${String(m+1).padStart(2,'0')}-${String(end.getDate()).padStart(2,'0')}`} }
  const start=new Date(y,m,day-d.getDay()+1); const end=new Date(y,m,day-d.getDay()+7)
  const iso=(x:Date)=>`${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,'0')}-${String(x.getDate()).padStart(2,'0')}`
  return {start:iso(start),end:iso(end)}
}

export default function App(){
  const [page,setPage]=useState<'home'|'add'|'list'|'stats'|'settings'>('home')
  const [transactions,setTransactions]=useState<Transaction[]>([]); const [categories,setCategories]=useState<Category[]>([]); const [members,setMembers]=useState<Member[]>([])
  const [syncing,setSyncing]=useState(false); const [online,setOnline]=useState(navigator.onLine); const [editing,setEditing]=useState<Transaction|null>(null); const [toast,setToast]=useState(''); const [adminMode,setAdminMode]=useState(Boolean(api.adminToken())); const [pendingCount,setPendingCount]=useState(0)
  const reloadLocal=async()=>{const [tx,cats,mem]=await Promise.all([db.transactions.filter(t=>!t.is_deleted).toArray(),db.categories.toArray(),db.members.toArray()]);tx.sort((a,b)=>b.date.localeCompare(a.date)||b.created_at.localeCompare(a.created_at));setTransactions(tx);setCategories(cats);setMembers(mem);setPendingCount(await db.outbox.count())}
  const sync=async()=>{if(!navigator.onLine||syncing)return;setSyncing(true);try{const outbox=await db.outbox.toArray();const result=await api.syncTransactions(outbox.map(x=>x.tx));await db.transaction('rw',db.transactions,db.categories,db.members,db.outbox,async()=>{await db.transactions.clear();await db.categories.clear();await db.members.clear();await db.transactions.bulkPut(result.transactions);await db.categories.bulkPut(result.categories);await db.members.bulkPut(result.members);await db.outbox.clear()});await reloadLocal();setToast(outbox.length?`已同步 ${outbox.length} 笔`:'数据已同步')}catch(e){setToast(e instanceof Error?e.message:'同步失败')}finally{setSyncing(false)}}
  useEffect(()=>{const boot=async()=>{await reloadLocal();if(navigator.onLine)await sync()};void boot();const onOnline=()=>{setOnline(true);void sync()};const onOffline=()=>setOnline(false);const onVisible=()=>{if(document.visibilityState==='visible'&&navigator.onLine)void sync()};window.addEventListener('online',onOnline);window.addEventListener('offline',onOffline);window.addEventListener('focus',onVisible);document.addEventListener('visibilitychange',onVisible);return()=>{window.removeEventListener('online',onOnline);window.removeEventListener('offline',onOffline);window.removeEventListener('focus',onVisible);document.removeEventListener('visibilitychange',onVisible)}},[])
  useEffect(()=>{if(toast){const t=setTimeout(()=>setToast(''),2200);return()=>clearTimeout(t)}},[toast])
  const saveLocalTransaction=async(tx:Transaction)=>{await db.transactions.put(tx);await db.outbox.put({tx});await reloadLocal();if(navigator.onLine)void sync()}
  const submitTx=async(draft:Partial<Transaction>,keepForm=false)=>{if(!draft.project?.trim()||!draft.member||!draft.amount_cents){setToast('请填写项目、成员和金额');return}const tx:Transaction={id:draft.id||uid(),project:draft.project.trim(),date:draft.date||today(),member:draft.member,type:(draft.type||'expense') as TxType,category1:draft.category1||null,category2:draft.category2||null,amount_cents:Number(draft.amount_cents),note:draft.note||null,created_at:draft.created_at||nowIso(),updated_at:nowIso(),device_id:draft.device_id||api.deviceId(),is_deleted:false};await saveLocalTransaction(tx);setToast('已保存');if(!keepForm)setPage('home');return tx}
  const doDelete=async(tx:Transaction)=>{const deleted={...tx,is_deleted:true,updated_at:nowIso()};await db.transactions.put(deleted);await db.outbox.put({tx:deleted});await reloadLocal();if(navigator.onLine)void sync();setToast('已删除')}
  const month=monthRange();const monthTx=useMemo(()=>transactions.filter(t=>t.date>=month.start&&t.date<=month.end),[transactions,month.start,month.end]);const monthExpense=sum(monthTx.filter(t=>t.type==='expense'));const monthIncome=sum(monthTx.filter(t=>t.type==='income'))
  const cat1=categories.filter(c=>c.type==='expense_category1'&&c.is_active);const cat2=categories.filter(c=>c.type==='expense_category2'&&c.is_active);const incomeCats=categories.filter(c=>c.type==='income_category'&&c.is_active)
  return <div className="app-shell"><header className="topbar"><div><strong>我的记账</strong><small>{online?(syncing?'同步中…':'已连接'):'离线模式'}{pendingCount?` · 待同步 ${pendingCount}`:''}</small></div><button className="icon-btn" onClick={sync} disabled={!online||syncing}>↻</button></header><main className="content">
    {page==='home'&&<HomePage monthExpense={monthExpense} monthIncome={monthIncome} transactions={transactions} members={members} onAdd={()=>{setEditing(null);setPage('add')}} onOpenTx={t=>{setEditing(t);setPage('add')}}/>}
    {page==='add'&&<AddPage editing={editing} members={members} cat1={cat1} cat2={cat2} incomeCats={incomeCats} onCancel={()=>setPage('home')} onSubmit={submitTx}/>} 
    {page==='list'&&<ListPage transactions={transactions} onEdit={t=>{setEditing(t);setPage('add')}} onDelete={doDelete} members={members}/>} 
    {page==='stats'&&<StatsPage transactions={transactions} members={members}/>} 
    {page==='settings'&&<SettingsPage categories={categories} members={members} adminMode={adminMode} setAdminMode={setAdminMode} onSync={sync} onToast={setToast}/>} 
  </main><nav className="tabbar"><Tab active={page==='home'} label="首页" icon="⌂" onClick={()=>setPage('home')}/><Tab active={page==='list'} label="账单" icon="▤" onClick={()=>setPage('list')}/><button className="fab" onClick={()=>{setEditing(null);setPage('add')}}>＋<span>记一笔</span></button><Tab active={page==='stats'} label="统计" icon="◒" onClick={()=>setPage('stats')}/><Tab active={page==='settings'} label="设置" icon="⚙" onClick={()=>setPage('settings')}/></nav>{toast&&<div className="toast">{toast}</div>}</div>
}
function Tab({active,label,icon,onClick}:{active:boolean,label:string,icon:string,onClick:()=>void}){return <button className={`tab ${active?'active':''}`} onClick={onClick}><span>{icon}</span><small>{label}</small></button>}

function HomePage({monthExpense,monthIncome,transactions,members,onAdd,onOpenTx}:{monthExpense:number,monthIncome:number,transactions:Transaction[],members:Member[],onAdd:()=>void,onOpenTx:(t:Transaction)=>void}){const accounts=members.filter(m=>m.is_active);return <><section className="hero-card"><div className="eyebrow">本月</div><div className="hero-number expense">{yuan(monthExpense)}</div><div className="hero-sub"><span className="income">收入 {yuan(monthIncome)}</span> · <span className="balance">结余 {yuan(monthIncome-monthExpense)}</span></div></section><section className="account-grid">{accounts.map(a=>{const e=sum(transactions.filter(t=>t.member===a.name&&t.type==='expense'&&t.date>=monthRange().start&&t.date<=monthRange().end));const i=sum(transactions.filter(t=>t.member===a.name&&t.type==='income'&&t.date>=monthRange().start&&t.date<=monthRange().end));const te=sum(transactions.filter(t=>t.member===a.name&&t.type==='expense'));const ti=sum(transactions.filter(t=>t.member===a.name&&t.type==='income'));const b=ti-te;return <div className="account-card" key={a.id}><div className="account-name">{a.name}</div><div><small>余额</small><strong className="balance">{b>=0?'+':'-'}{yuan(Math.abs(b))}</strong></div><div><small>本月支出</small><strong className="expense">-{yuan(e)}</strong></div><div><small>本月收入</small><strong className="income">+{yuan(i)}</strong></div></div>})}</section><button className="primary wide" onClick={onAdd}>＋ 记一笔</button><section className="panel"><div className="panel-title">最近 5 笔</div>{transactions.slice(0,5).map(t=><TxRow key={t.id} tx={t} onClick={()=>onOpenTx(t)}/>)}{!transactions.length&&<div className="empty">还没有记录，先记第一笔。</div>}</section></>}

function AddPage({editing,members,cat1,cat2,incomeCats,onCancel,onSubmit}:{editing:Transaction|null,members:Member[],cat1:Category[],cat2:Category[],incomeCats:Category[],onCancel:()=>void,onSubmit:(d:Partial<Transaction>,keep?:boolean)=>Promise<Transaction|undefined>}){const [form,setForm]=useState<Partial<Transaction>>(editing?{...editing}:defaultTx());useEffect(()=>{if(!form.member&&members[0])setForm(f=>({...f,member:members[0].name}))},[members,form.member]);const set=(k:keyof Transaction,v:unknown)=>setForm(f=>({...f,[k]:v}));const submit=async(keep=false)=>{await onSubmit(form,keep);if(keep)setForm(f=>({...defaultTx(),member:f.member,date:f.date,category1:f.category1,category2:f.category2}))};const income=form.type==='income';return <><div className="page-head"><button className="back" onClick={onCancel}>‹</button><h2>{editing?'编辑记录':'记一笔'}</h2></div><div className="form panel"><label>项目 / 用途<input autoFocus value={form.project||''} onChange={e=>set('project',e.target.value)} placeholder="例如：买菜、工资"/></label><div className="segmented"><button className={!income?'selected':''} onClick={()=>set('type','expense')}>支出</button><button className={income?'selected':''} onClick={()=>set('type','income')}>收入</button></div><label>日期<input type="date" value={form.date||today()} onChange={e=>set('date',e.target.value)}/></label><label>账户<select value={form.member||''} onChange={e=>set('member',e.target.value)}>{members.filter(m=>m.is_active).map(m=><option key={m.id}>{m.name}</option>)}</select></label><label>金额<input inputMode="decimal" type="number" step="0.01" min="0" value={form.amount_cents?(form.amount_cents/100).toString():''} onChange={e=>set('amount_cents',Math.round(Number(e.target.value||0)*100))} placeholder="0.00"/></label><label>{income?'收入分类':'分类1'}<select value={form.category1||''} onChange={e=>set('category1',e.target.value)}><option value="">请选择</option>{(income?incomeCats:cat1).map(c=><option key={c.id}>{c.name}</option>)}</select></label>{!income&&<label>分类2<select value={form.category2||''} onChange={e=>set('category2',e.target.value)}><option value="">请选择</option>{cat2.map(c=><option key={c.id}>{c.name}</option>)}</select></label>}<label>备注<textarea value={form.note||''} onChange={e=>set('note',e.target.value)} placeholder="可选" rows={3}/></label><button className="primary wide" onClick={()=>submit(false)}>保存</button>{!editing&&<button className="secondary wide" onClick={()=>submit(true)}>保存并继续</button>}{editing&&<button className="secondary wide" onClick={onCancel}>取消</button>}</div></>}

function TxRow({tx,onClick}:{tx:Transaction,onClick?:()=>void}){return <button className="tx-row" onClick={onClick}><div><strong>{tx.project}</strong><small>{tx.date} · {tx.member}{tx.category1?` · ${tx.category1}`:''}{tx.category2?` · ${tx.category2}`:''}</small></div><strong className={tx.type==='expense'?'expense':'income'}>{tx.type==='expense'?'-':'+'}{yuan(tx.amount_cents)}</strong></button>}
function ListPage({transactions,onEdit,onDelete,members}:{transactions:Transaction[],onEdit:(t:Transaction)=>void,onDelete:(t:Transaction)=>void,members:Member[]}){const[q,setQ]=useState('');const[type,setType]=useState('');const[member,setMember]=useState('');const filtered=transactions.filter(t=>(!q||`${t.project} ${t.note||''}`.includes(q))&&(!type||t.type===type)&&(!member||t.member===member));return <><div className="page-head"><h2>账单</h2></div><div className="filters"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="搜索项目、备注"/><select value={type} onChange={e=>setType(e.target.value)}><option value="">全部类型</option><option value="expense">支出</option><option value="income">收入</option></select><select value={member} onChange={e=>setMember(e.target.value)}><option value="">全部账户</option>{members.filter(m=>m.is_active).map(m=><option key={m.id}>{m.name}</option>)}</select></div><section className="panel">{filtered.map(t=><div key={t.id} className="tx-wrap"><TxRow tx={t} onClick={()=>onEdit(t)}/><button className="delete-link" onClick={e=>{e.stopPropagation();if(confirm('删除这笔记录？'))onDelete(t)}}>删除</button></div>)}{!filtered.length&&<div className="empty">没有匹配记录。</div>}</section></>}

function SummaryCard({label,tx}:{label:string,tx:Transaction[]}){const e=sum(tx.filter(t=>t.type==='expense'));const i=sum(tx.filter(t=>t.type==='income'));return <div className="period-card"><div className="period-title">{label}</div><div><span className="expense">支出 {yuan(e)}</span></div><div><span className="income">收入 {yuan(i)}</span></div><div><span className="balance">结余 {yuan(i-e)}</span></div></div>}
function StatsPage({transactions,members}:{transactions:Transaction[],members:Member[]}){const [start,setStart]=useState(monthRange().start);const [end,setEnd]=useState(monthRange().end);const [account1,setAccount1]=useState('ALL');const [account2,setAccount2]=useState('ALL');const tx=transactions.filter(t=>t.date>=start&&t.date<=end);const expense=sum(tx.filter(t=>t.type==='expense'));const income=sum(tx.filter(t=>t.type==='income'));const filtered1=account1==='ALL'?tx:tx.filter(t=>t.member===account1);const filtered2=account2==='ALL'?tx:tx.filter(t=>t.member===account2);return <><div className="page-head"><h2>统计</h2></div><div className="period-cards"><SummaryCard label="本周" tx={transactions.filter(t=>{const r=periodRange('week');return t.date>=r.start&&t.date<=r.end})}/><SummaryCard label="本月" tx={transactions.filter(t=>t.date>=periodRange('month').start&&t.date<=periodRange('month').end)}/><SummaryCard label="本年" tx={transactions.filter(t=>t.date>=periodRange('year').start&&t.date<=periodRange('year').end)}/></div><div className="date-range"><input aria-label="开始日期" type="date" value={start} onChange={e=>setStart(e.target.value)}/><span className="date-sep">至</span><input aria-label="结束日期" type="date" value={end} onChange={e=>setEnd(e.target.value)}/></div><div className="two-cards"><div className="stat-card"><small>总支出</small><strong className="expense">{yuan(expense)}</strong></div><div className="stat-card"><small>总收入</small><strong className="income">{yuan(income)}</strong></div></div><div className="two-cards"><div className="stat-card"><small>净结余</small><strong className="balance">{yuan(income-expense)}</strong></div><div className="stat-card"><small>记录数</small><strong>{tx.length}</strong></div></div><PieSection title="支出分类1" data={groupBy(filtered1,'category1')} account={account1} setAccount={setAccount1} members={members}/><PieSection title="支出分类2" data={groupBy(filtered2,'category2')} account={account2} setAccount={setAccount2} members={members}/><section className="panel chart"><div className="panel-title">按账户</div><AccountBarChart data={members.filter(m=>m.is_active).map(m=>({name:m.name,支出:Math.round(sum(tx.filter(t=>t.member===m.name&&t.type==='expense'))/100),收入:Math.round(sum(tx.filter(t=>t.member===m.name&&t.type==='income'))/100)}))}/></section></>}
function groupBy(tx:Transaction[],field:'category1'|'category2'){const map:Record<string,number>={};tx.filter(t=>t.type==='expense').forEach(t=>{const k=t[field]||'未分类';map[k]=(map[k]||0)+t.amount_cents});return Object.entries(map).map(([name,value])=>({name,value:value/100})).sort((a,b)=>b.value-a.value)}
function PieSection({title,data,account,setAccount,members}:{title:string,data:{name:string,value:number}[],account:string,setAccount:(v:string)=>void,members:Member[]}){const total=data.reduce((s,d)=>s+d.value,0);return <section className="panel chart"><div className="chart-head"><div className="panel-title">{title}</div><select value={account} onChange={e=>setAccount(e.target.value)}><option value="ALL">账户及合计</option>{members.filter(m=>m.is_active).map(m=><option key={m.id} value={m.name}>{m.name}</option>)}</select></div>{data.length?<><div className="pie-wrap"><ResponsiveContainer width="100%" height={250}><PieChart><Pie data={data} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={88} innerRadius={35} paddingAngle={2} label={(p:any)=>`${moneyLabel(Number(p.value))}`} labelLine={false}>{data.map((_,i)=><Cell key={i} fill={PIE_COLORS[i%PIE_COLORS.length]}/>)}</Pie><Tooltip formatter={(v)=>moneyLabel(Number(v))}/></PieChart></ResponsiveContainer></div><div className="pie-legend">{data.map((item,i)=><div className="pie-legend-row" key={item.name}><span className="legend-dot" style={{background:PIE_COLORS[i%PIE_COLORS.length]}}/><span className="legend-name">{item.name}</span><span className="legend-value">{moneyLabel(item.value)} · {total?Math.round(item.value/total*100):0}%</span></div>)}</div></>:<div className="empty">当前区间没有支出。</div>}</section>}
function AccountBarChart({data}:{data:{name:string,支出:number,收入:number}[]}){const max=Math.max(1,...data.flatMap(d=>[d.支出,d.收入]));const nice=Math.ceil(max/500)*500||500;return <div className="account-bar-chart"><div className="chart-plot"><div className="chart-y-labels">{[1,.75,.5,.25,0].map((r,i)=><span key={i}>{moneyLabel(nice*r)}</span>)}</div><div className="chart-grid-area"><div className="chart-grid-lines">{[1,.75,.5,.25,0].map((_,i)=><span key={i}/>)}</div><div className="chart-groups">{data.map(d=><div className="chart-group" key={d.name}><div className="chart-bars"><div className="chart-bar-col"><div className="chart-value">{moneyLabel(d.支出)}</div><div className="chart-bar expense-bar" style={{height:`${Math.max(d.支出?4:0,d.支出/nice*100)}%`}}/></div><div className="chart-bar-col"><div className="chart-value">{moneyLabel(d.收入)}</div><div className="chart-bar income-bar" style={{height:`${Math.max(d.收入?4:0,d.收入/nice*100)}%`}}/></div></div><div className="chart-x-label">{d.name}</div></div>)}</div></div></div><div className="chart-legend"><span><i className="legend-square expense-square"/>支出</span><span><i className="legend-square income-square"/>收入</span></div></div>}

function SettingsPage({categories,members,adminMode,setAdminMode,onSync,onToast}:{categories:Category[],members:Member[],adminMode:boolean,setAdminMode:(v:boolean)=>void,onSync:()=>Promise<void>,onToast:(s:string)=>void}){const[password,setPassword]=useState('');const doLogin=async()=>{try{await api.loginAdmin(password);setAdminMode(true);onToast('管理员模式已开启')}catch(e){onToast(e instanceof Error?e.message:'登录失败')}};return <><div className="page-head"><h2>设置</h2></div>{!adminMode&&<section className="panel"><div className="panel-title">管理员</div><div className="hint">普通用户可以记账和查看，分类/成员管理、导入导出需要管理员密码。</div><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="管理员密码"/><button className="primary wide" onClick={doLogin}>进入管理员模式</button></section>}{adminMode&&<AdminPanel categories={categories} members={members} onSync={onSync} onToast={onToast}/>}<section className="panel"><div className="panel-title">设备</div><div className="hint">设备 ID：{api.deviceId()}</div><button className="secondary wide" onClick={()=>{localStorage.removeItem('expense-admin-token');setAdminMode(false);onToast('已退出管理员模式')}}>退出管理员模式</button></section></>}
function AdminPanel({categories,members,onSync,onToast}:{categories:Category[],members:Member[],onSync:()=>Promise<void>,onToast:(s:string)=>void}){const[newCat,setNewCat]=useState('');const[catType,setCatType]=useState<'expense_category1'|'expense_category2'|'income_category'>('expense_category1');const[newMember,setNewMember]=useState('');const fileRef=useRef<HTMLInputElement>(null);const addCat=async()=>{try{if(!newCat.trim())return;await api.addCategory({name:newCat.trim(),type:catType,sort_order:100});setNewCat('');await onSync();onToast('分类已添加')}catch(e){onToast(e instanceof Error?e.message:'添加失败')}};const addMem=async()=>{try{if(!newMember.trim())return;await api.addMember({name:newMember.trim(),sort_order:100});setNewMember('');await onSync();onToast('成员已添加')}catch(e){onToast(e instanceof Error?e.message:'添加失败')}};const doExport=async()=>{try{const blob=await api.exportCsv();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='expense_export.csv';a.click();URL.revokeObjectURL(url);onToast('导出完成')}catch(e){onToast(e instanceof Error?e.message:'导出失败')}};const doImport=async(e:React.ChangeEvent<HTMLInputElement>)=>{const file=e.target.files?.[0];if(!file)return;try{const result=await api.importCsv(await file.text());await onSync();onToast(result.errors.length?`导入 ${result.imported} 笔，${result.errors.length} 行失败`:`已导入 ${result.imported} 笔`)}catch(err){onToast(err instanceof Error?err.message:'导入失败')}finally{e.target.value=''}};return <section className="panel admin"><div className="panel-title">管理员</div><div className="admin-block"><strong>数据导入导出</strong><div className="import-export"><button className="secondary" onClick={doExport}>导出 CSV</button><button className="secondary" onClick={()=>fileRef.current?.click()}>导入 CSV</button><input ref={fileRef} type="file" accept=".csv,text/csv" hidden onChange={doImport}/></div><div className="hint">格式：项目、分类、分类2、年份函数、日期、月份函数、账户、金额。导出文件可直接再次导入。</div></div><div className="admin-block"><strong>新增分类</strong><div className="inline"><select value={catType} onChange={e=>setCatType(e.target.value as typeof catType)}><option value="expense_category1">支出分类1</option><option value="expense_category2">支出分类2</option><option value="income_category">收入分类</option></select><input value={newCat} onChange={e=>setNewCat(e.target.value)} placeholder="分类名称"/><button className="secondary" onClick={addCat}>添加</button></div></div><div className="admin-block"><strong>新增成员</strong><div className="inline"><input value={newMember} onChange={e=>setNewMember(e.target.value)} placeholder="姓名（最多 5 人）"/><button className="secondary" onClick={addMem}>添加</button></div></div><div className="admin-block"><strong>现有分类</strong>{categories.map(c=><div className="manage-row" key={c.id}><span>{c.name}<small>{c.type==='expense_category1'?'支出分类1':c.type==='expense_category2'?'支出分类2':c.type==='income_category'?'收入分类':'旧数据'}</small></span><button className="delete-link" onClick={async()=>{try{await api.deleteCategory(c.id);await onSync();onToast('已删除')}catch(e){onToast(e instanceof Error?e.message:'删除失败')}}}>删除</button></div>)}</div><div className="admin-block"><strong>成员</strong>{members.map(m=><div className="manage-row" key={m.id}><span>{m.name}</span><button className="delete-link" onClick={async()=>{try{await api.deleteMember(m.id);await onSync();onToast('已删除')}catch(e){onToast(e instanceof Error?e.message:'删除失败')}}}>删除</button></div>)}</div><div className="admin-block"><strong>SQLite 备份</strong><a className="secondary link-btn" href="/api/backup">下载数据库备份</a></div></section>}
